#######This algorithm could take huge time, thus parallel computing is required.
#######May not work on Windows environment (see R package 'parallel' for more information).

library(e1071)
library(parallel)
